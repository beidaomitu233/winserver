<Window x:Class="XpCnLocalManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Codex Server Control"
        Width="1160"
        Height="760"
        MinWidth="980"
        MinHeight="680"
        WindowStartupLocation="CenterScreen"
        FontFamily="{StaticResource AppFont}">
  <Window.Resources>
    <Style x:Key="NavButtonStyle" TargetType="Button">
      <Setter Property="Height" Value="48" />
      <Setter Property="Margin" Value="12,2" />
      <Setter Property="Padding" Value="18,0" />
      <Setter Property="HorizontalContentAlignment" Value="Left" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="FontSize" Value="15" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Border"
                    CornerRadius="8"
                    Background="{TemplateBinding Background}">
              <ContentPresenter VerticalAlignment="Center"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                Margin="{TemplateBinding Padding}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Border" Property="Opacity" Value="0.88" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
      <Setter Property="Height" Value="34" />
      <Setter Property="Padding" Value="14,0" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="Background" Value="#188CE5" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Border"
                    Background="{TemplateBinding Background}"
                    CornerRadius="7">
              <ContentPresenter HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Margin="{TemplateBinding Padding}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Border" Property="Background" Value="#279BEE" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Border" Property="Opacity" Value="0.45" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="GhostButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
      <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
      <Setter Property="Background" Value="{DynamicResource GhostButtonBrush}" />
    </Style>
  </Window.Resources>

  <Border Background="{DynamicResource AppBackgroundBrush}">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="248" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Border Grid.Column="0"
              Background="{DynamicResource SidebarBrush}"
              BorderBrush="{DynamicResource BorderBrush}"
              BorderThickness="0,0,1,0">
        <DockPanel>
          <StackPanel DockPanel.Dock="Top" Margin="22,22,22,16">
            <Border Width="42"
                    Height="42"
                    HorizontalAlignment="Left"
                    CornerRadius="10"
                    Background="#188CE5">
              <TextBlock Text="P"
                         Foreground="White"
                         FontWeight="SemiBold"
                         FontSize="25"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center" />
            </Border>
            <TextBlock Text="Codex Server"
                       Margin="0,14,0,0"
                       Foreground="{DynamicResource TextBrush}"
                       FontSize="20"
                       FontWeight="SemiBold" />
            <TextBlock Text="Local web stack manager"
                       Margin="0,4,0,0"
                       Foreground="{DynamicResource MutedBrush}"
                       FontSize="12" />
          </StackPanel>

          <StackPanel x:Name="NavPanel"
                      DockPanel.Dock="Top"
                      Margin="0,6,0,0" />

          <Border DockPanel.Dock="Bottom"
                  Margin="18"
                  Padding="14"
                  CornerRadius="10"
                  Background="{DynamicResource CardBrush}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  BorderThickness="1">
            <StackPanel>
              <TextBlock Text="Runtime"
                         Foreground="{DynamicResource MutedBrush}"
                         FontSize="12" />
              <TextBlock x:Name="RuntimeText"
                         Text="Checking services..."
                         TextWrapping="Wrap"
                         Margin="0,7,0,0"
                         Foreground="{DynamicResource TextBrush}"
                         FontSize="12" />
            </StackPanel>
          </Border>
        </DockPanel>
      </Border>

      <Grid Grid.Column="1">
        <Grid.RowDefinitions>
          <RowDefinition Height="64" />
          <RowDefinition Height="*" />
          <RowDefinition Height="36" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1">
          <DockPanel Margin="22,0">
            <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Right"
                        VerticalAlignment="Center">
              <Button x:Name="ThemeButton"
                      Content="浅色"
                      Width="74"
                      Margin="0,0,10,0"
                      Style="{StaticResource GhostButtonStyle}"
                      Click="ThemeButton_Click" />
              <Button Content="最小化"
                      Width="82"
                      Style="{StaticResource GhostButtonStyle}"
                      Click="MinimizeButton_Click" />
            </StackPanel>
            <StackPanel VerticalAlignment="Center">
              <TextBlock x:Name="PageTitle"
                         Text="首页"
                         Foreground="{DynamicResource TextBrush}"
                         FontSize="19"
                         FontWeight="SemiBold" />
              <TextBlock Text="Manage Apache, Nginx, MySQL, Redis and MinIO without leaving your desktop."
                         Foreground="{DynamicResource MutedBrush}"
                         FontSize="12"
                         Margin="0,4,0,0" />
            </StackPanel>
          </DockPanel>
        </Border>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
          <Grid x:Name="ContentHost" Margin="22" />
        </ScrollViewer>

        <Border Grid.Row="2"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0">
          <DockPanel Margin="22,0">
            <TextBlock Text="版本：8.1.1.3-local"
                       DockPanel.Dock="Right"
                       Foreground="{DynamicResource MutedBrush}"
                       VerticalAlignment="Center" />
            <TextBlock x:Name="StatusText"
                       Foreground="{DynamicResource MutedBrush}"
                       VerticalAlignment="Center" />
          </DockPanel>
        </Border>
      </Grid>
    </Grid>
  </Border>
</Window>
